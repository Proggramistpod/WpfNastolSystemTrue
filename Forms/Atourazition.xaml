<Window x:Class="WpfNastolSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему"
        Height="520"
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#EEF6FF">
    <Grid>
        <Border Style="{StaticResource PanelStyle}"
                Width="420"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel Margin="0,20,0,40">
                <TextBlock Text="Вход в систему"
                           Style="{StaticResource PageTitleStyle}"/>
                <Grid Height="62" Margin="0,0,0,20">
                    <Border x:Name="LoginBorder" Style="{StaticResource InputBorderStyle}"/>
                    <TextBlock x:Name="HintLogin"
                               Text="Логин или email"
                               Style="{StaticResource FloatingHintTextBoxStyle}"
                               IsHitTestVisible="False">
                        <TextBlock.RenderTransform>
                            <TranslateTransform x:Name="LoginTransform"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                    <TextBox x:Name="InputLogin"
                             Padding="16,12,16,0"
                             FontSize="15"
                             Background="Transparent"
                             BorderThickness="0"
                             VerticalContentAlignment="Bottom"
                             CaretBrush="{StaticResource PrimaryBlue}"
                             Margin="0,0,0,10"/>
                </Grid>
                <Grid Height="62" Margin="0,0,0,32">
                    <Border x:Name="PasswordBorder" Style="{StaticResource InputBorderStyle}"/>
                    <TextBlock x:Name="HintPassword"
                               Text="Пароль"
                               Style="{StaticResource FloatingHintTextBoxStyle}"
                               IsHitTestVisible="False">
                        <TextBlock.RenderTransform>
                            <TranslateTransform x:Name="PasswordTransform"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                    <PasswordBox x:Name="InputPassword"
                                 Padding="16,12,12,0"
                                 FontSize="15"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 VerticalContentAlignment="Bottom"
                                 CaretBrush="{StaticResource PrimaryBlue}"
                                 Margin="0,0,0,9"/>
                </Grid>
                <Button Content="Войти"
                        Height="52"
                        Style="{StaticResource CustomBlueButtonStyle}"
                        Click="Button_Click"/>
                <Button Content="Забыли пароль?"
                           FontSize="13"
                           Foreground="{StaticResource PrimaryBlue}"
                           HorizontalAlignment="Right"
                           Margin="0,16,0,0"
                           Style="{StaticResource SecondaryButtonStyle}" Height="18"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>